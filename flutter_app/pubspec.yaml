name: restaurant_creatives
description: Restaurant Menu to Social Media Creatives Generator
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  provider: ^6.1.0
  flutter_dotenv: ^5.1.0
  cached_network_image: ^3.3.0
  path_provider: ^2.1.0
  dio: ^5.4.0
  archive: ^3.4.0
  url_launcher: ^6.2.0
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
